# duplicate element reaarangement in an array
# Unique Elements in Sorted Array

This program removes duplicates from a sorted array in-place using two pointers (`i` and `j`).  
The variable `res` keeps track of the count of unique elements found.  
Final output prints both the count and the unique elements.
time complexity -> O(n) space complexity -> O(1)